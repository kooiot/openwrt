--- a/drivers/net/ethernet/stmicro/stmmac/stmmac.h
+++ b/drivers/net/ethernet/stmicro/stmmac/stmmac.h
@@ -363,6 +363,9 @@ struct stmmac_priv {
 	/* XDP BPF Program */
 	unsigned long *af_xdp_zc_qps;
 	struct bpf_prog *xdp_prog;
+
+	/* For unregister_netdev crash */
+	int netdev_inited;
 };

 enum stmmac_state {
--- a/drivers/net/ethernet/stmicro/stmmac/stmmac_main.c
+++ b/drivers/net/ethernet/stmicro/stmmac/stmmac_main.c
@@ -7775,6 +7775,7 @@ int stmmac_dvr_probe(struct device *devi
 			__func__, ret);
 		goto error_netdev_register;
 	}
+	priv->netdev_inited = 1;

 #ifdef CONFIG_DEBUG_FS
 	stmmac_init_fs(ndev);
@@ -7822,7 +7823,9 @@ void stmmac_dvr_remove(struct device *de

 	pm_runtime_get_sync(dev);

-	unregister_netdev(ndev);
+	if (priv->netdev_inited)
+		unregister_netdev(ndev);
+	priv->netdev_inited = 0;

 #ifdef CONFIG_DEBUG_FS
 	stmmac_exit_fs(ndev);
